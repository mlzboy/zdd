<h2>忘记密码</h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <p><%= f.label :email,"注册邮箱："%><br />
  <%= f.email_field :email %></p>

  <p><%= f.submit "发送" %></p>
<% end %>

<%= render :partial => "devise/shared/links" %>